services:
  api:
    build:
      context: ./api
    ports:
      - "8111:8000"
    env_file:
      - ./api/.env

  web:
    build:
      context: ./web
    ports:
      - "3111:3000"
    depends_on:
      api:
        condition: service_started
